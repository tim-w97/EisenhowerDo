import {StyleSheet} from 'react-native';

const globalStyles = StyleSheet.create({
  safeArea: {
    padding: 20,
  },

  bigTitle: {
    fontWeight: 'bold',
    fontSize: 20,
  },
});

export default globalStyles;
