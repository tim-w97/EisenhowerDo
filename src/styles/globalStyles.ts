import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  safeArea: {
    flex: 1,
    padding: 20,
  },

  bigTitle: {
    fontWeight: 'bold',
    fontSize: 20,
  },

  textInput: {
    borderWidth: 5,
    borderRadius: 5,
    borderColor: 'lightblue',
    padding: 15,
  },

  actionButton: {
    padding: 10,
    color: 'blue',
  },
});
